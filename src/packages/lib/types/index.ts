/**
 * Barrel — UI Types
 * =============================================================================
 * Single import point for UI-only type aliases. Intentionally *thin*:
 * everything here references the runtime schema (SSOT) where possible.
 *
 * Example:
 *   import type { PackageRuntime, Money, BandVariant, CtaPair } from "@/packages/lib/types";
 */

export * from "./types";
export * from "./pricing";
export * from "./copy";
export * from "./band";
