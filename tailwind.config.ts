// tailwind.config.ts
import type { Config } from 'tailwindcss';
export default {
  theme: {
    extend: {
      container: { center: true, padding: '1rem' },
    },
  },
  plugins: [],
} satisfies Config;
